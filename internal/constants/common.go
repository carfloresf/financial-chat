package constants

// Userkey is the key used to store the user in the session
const Userkey = "user"

// CommandStock is the command to get stock
const CommandStock = "/stock="

// CommandHelp is the command to get help
const CommandHelp = "/help"
